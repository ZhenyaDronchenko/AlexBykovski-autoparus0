{% extends 'base.html.twig' %}

{% block title %}{{ page.title }}{% endblock %}
{% block meta_description %}{{ page.description }}{% endblock %}

{% block stylesheets %}
    <link rel='stylesheet' href='/bower-components/owl.carousel/dist/assets/owl.carousel.min.css'>
    <link rel='stylesheet' href='/bower-components/owl.carousel/dist/assets/owl.theme.default.min.css'>
    <link rel='stylesheet' href='/css/catalog/auto/obd2-error/choice-type.css'>
{% endblock %}

{% block body %}
    <main class="container">
        {% include('/client/default/parts/article-top-carousel.html.twig') with {"articles" : articles} %}
        <div class="inner-grey">
        <ul id="breadcrumbs" itemscope itemtype="http://schema.org/BreadcrumbList">
            <li itemprop="itemListElement" itemscope itemtype="http://schema.org/ListItem">
                <a class="user-link user-link--icon" href="{{ path("homepage") }}" title="{{ titleHomepage }}" itemprop="item">
                    <span class="user-name" itemprop="name">Главная</span>
                    <svg class="user-icon" x="0px" y="0px" viewBox="0 0 16 16" width="20px" height="20px">
                        <polyline fill="none" stroke="#444" stroke-miterlimit="10" points="1,8.5 8,2 15,8.5 "/>
                        <polyline fill="none" stroke="#444" stroke-miterlimit="10" points="2.5,7 2.5,13.5 6.5,13.5 6.5,8.5 9.5,8.5 9.5,13.5   13.5,13.5 13.5,7 "/>
                    </svg>
                    <meta itemprop="position" content="1">
                </a>
            </li>
            <li itemprop="itemListElement" itemscope itemtype="http://schema.org/ListItem">
                <a class="current" itemprop="item" itemprop="item"><span class="line">/</span><span itemprop="name">Ошибки работы электронных систем автомобиля</span>
                    <meta itemprop="position" content="2"></a></li>
        </ul>

        <section class="engine">
            <h1 class="title">{{ page.headline1 }}</h1>
            <div class="text">{{ page.text1 | raw }}</div>
            <ul class="engine-block">
                {% for key, type in types if not (key in ['P', 'B', 'C', 'U']) %}
                    <li><a class="engine-link" title="{{ type["type"] }}" href="{{ path("show_obd2_error_catalog_choice_code", {urlType: type["url"]}) }}">{{ key }}</a></li>
                {% endfor %}
            </ul>
            <ul class="error-list">
                {% for key, type in types %}
                    {% if key == 'P' %}
                        <li><a href="{{ path("show_obd2_error_catalog_choice_code", {urlType: type["url"]}) }}" title="{{ type["type"] }}" class="error-item">Ошибка "Р"</a></li>
                    {% elseif key == 'B' %}
                        <li><a href="{{ path("show_obd2_error_catalog_choice_code", {urlType: type["url"]}) }}" title="{{ type["type"] }}" class="error-item">Ошибка "B"</a></li>
                    {% elseif key == 'C' %}
                        <li><a href="{{ path("show_obd2_error_catalog_choice_code", {urlType: type["url"]}) }}" title="{{ type["type"] }}" class="error-item">Ошибка "C"</a></li>
                    {% elseif key == 'U' %}
                        <li><a href="{{ path("show_obd2_error_catalog_choice_code", {urlType: type["url"]}) }}" title="{{ type["type"] }}" class="error-item">Ошибка "U"</a></li>
                    {% endif %}
                {% endfor %}
            </ul>
            </section>
            <section class="engine-b">
            <h2 class="subtitle">{{ page.headline2 }}</h2>
            <div class="text text-m">{{ page.text2 | raw }}</div>
        </section>
        </div>
    </main>
{% endblock %}

{% block javascripts %}
    <script src='/bower-components/owl.carousel/dist/owl.carousel.min.js'></script>
{% endblock %}